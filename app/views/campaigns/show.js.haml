if("#{@background_process}" != "waiting"){
$('#campaigns_list').html("#{j render :partial => 'crafts/craft_list', :locals => {:campaign => @campaign, :repo => @repo, :repo_status => @repo_status, :new_and_changed => @new_and_changed, :current_project => @current_project, :saves => @saves, :most_recent_commit => @most_recent_commit, :deleted_craft_count => @deleted_craft_count, :craft_for_list => @craft_for_list, :campaign_commit_messages => @campaign_commit_messages, :current_project_commit_messages => @current_project_commit_messages, :current_project_history => @current_project_history} }");


$('#campaign_saves').html($('#content_for_campaign_saves').html());
$('#content_for_campaign_saves').html('')
if($('#active_save_display').val() == 'persistent'){
$('#quicksave_display').hide();
$('#persistent_display').show();
};

if($('#active_display').val() == 'saves'){
$('#current_project').hide();
$('#campaign_saves').show();

}else{
$('#campaign_saves').hide();
$('#current_project').show();
};



var current_project = $('#content_for_current_project').html()
$('#current_project').html(current_project)

$('#vab_list').html( $('#content_for_vab_list').html() )
$('#vab_list').parents('.grid_5').show();

$('#sph_list').html( $('#content_for_sph_list').html() )
$('#sph_list').parents('.grid_5').show();

$('#loading_holder').hide();


var show_del = $('#show_deleted').val()
if(show_del == "false"){
$(".deleted_craft").each(function(){$(this).hide()});
$('.show_del_craft_marker').removeClass("selected_marker");
$('.toggle_deleted_craft_link').html("show deleted craft")
}else{
$(".deleted_craft").each(function(){$(this).show()});
$('.show_del_craft_marker').addClass("selected_marker");
$('.toggle_deleted_craft_link').html("hide deleted craft")
};

};

show_help('craft_list_page_first_view', {close: function(){
setTimeout(function(){ show_help('craft_list_page_first_view_basic', {close: function(){
setTimeout(function(){ show_help('craft_list_page_first_view_summary')}, 200);
}}) },200);
}})

%h2 Instance
%h3= File.join(JSON.parse(@instance.full_path)).inspect

- sort_by_opt = :updated_at
- @campaigns.each do |campaign|
  %h4= campaign.name
  #craft_list
    = render('craft/craft_list', :campaign => campaign)

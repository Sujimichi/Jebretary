#standard_page
  %h1 All Instances
  .vblk_3
  .grid_8.push_2
    - @instances.each do |instance|
      .instance
        .left
          %h3= instance.path
        .right
          = link_to "Show", instance_path(instance)
          = link_to 'Destroy', instance, :method => :delete, :data => { :confirm => 'This will remove the reference to this install of KSP and all campaign and craft data.  (It will not effect your actual KSP install).  Are you sure you want to delete this?' }
        .clear
        -unless instance.exists?
          %h4= "This Instance can not be found and is being ignored"
